// frontend/src/admin/components/charts/VoteDistributionBar.tsx
// STUB — real implementation lands in Task 15. Empty render here lets
// the Statistics page mount without Task 15 having shipped yet.
import type { VoteDistributionRow } from "../../hooks/useStatistics";

interface Props {
  rows: VoteDistributionRow[];
}

export function VoteDistributionBar(_props: Props): JSX.Element {
  return <div />;
}
